# Floreria Florentina Landing Page v7

Mother's Day weekend maintenance page using the premium Floreria photo layout, with phone number and walk-in order notice.

## Update GitHub + Render

```bash
cd ~/Downloads/floreria_landing_page_v7
git init
git add .
git commit -m "Update v7 Mother's Day walk-in landing page"
git branch -M main
git remote add origin https://github.com/ryanstupart/floreria-florentina-landing.git
git push -f origin main
```

Render should auto-redeploy after the push.
